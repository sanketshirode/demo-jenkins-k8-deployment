apiVersion: v1
kind: Pod
metadata:
  labels:
    agent: pod

spec:
  containers:
    - name: docker
      image: docker:19.03
      command:
        - cat
      tty: true
      privileged: true
      volumeMounts:
        - name: dockersock
          mountPath: /var/run/docker.sock
    - name: kubectl
      image: bitnami/kubectl:latest
      command:
        - cat
      tty: true
      privileged: true
  volumes:
    - name: dockersock
      hostPath:
        path: /var/run/docker.sock
